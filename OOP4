#include <iostream>
using namespace std;
int sum(int a,int b,int c=0)
{
    return (a+b+c);
}
int product(int a,int b,int c=1)
{
    return (a*b*c);
}
int main()
{
    int a=7, b=8;
    cout<<"sum of "<<a<<" "<<b<<" :"<<sum(a,b)<<endl;
    int c=2;
    cout<<"Product of "<<a<<" "<<b<<" "<<c<<" :"<<product(a,b,c)<<endl;
}
